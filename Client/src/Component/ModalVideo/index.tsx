import { FC } from "react";
import "./index.less";
interface Prop {
  id: string;
}
const ModalVideo: FC<Prop> = ({ id }) => {
  return <div className="modal_video">index</div>;
};

export default ModalVideo;
